version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-lxion}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lxion_secret_2026}
      POSTGRES_DB: ${POSTGRES_DB:-lxion_db}
      TZ: Asia/Jakarta
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lxion} -d ${POSTGRES_DB:-lxion_db}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    environment:
      TZ: Asia/Jakarta
    expose:
      - "6379"
    volumes:
      - redis_data:/data

  whatsapp-sidecar:
    build:
      context: ./sidecar/whatsapp
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      PORT: "3001"
      CORE_URL: "http://lxion-core:8000"
      TZ: Asia/Jakarta
    expose:
      - "3001"
    volumes:
      - ./workspace/whatsapp_sessions:/app/workspace/whatsapp_sessions

  lxion-core:
    image: lxion-core:v0.3.6
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      whatsapp-sidecar:
        condition: service_started
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_USER=lxion
      - POSTGRES_PASSWORD=lxion_secret_2026
      - POSTGRES_DB=lxion_db
      - DATABASE_URL=postgresql+asyncpg://lxion:lxion_secret_2026@postgres:5432/lxion_db
      - REDIS_HOST=redis
      - REDIS_URL=redis://redis:6379/0
      - TZ=Asia/Jakarta
      - TIMEZONE=Asia/Jakarta
      - WHATSAPP_SIDECAR_URL=http://whatsapp-sidecar:3001
    expose:
      - "8000"
    volumes:
      - ./workspace:/app/workspace
      - ./credentials:/app/credentials
      - ./whatsapp_sessions:/app/whatsapp_sessions
      - ./logs:/app/logs
      - agents_data:/app/.agents

volumes:
  postgres_data:
  redis_data:
  agents_data: